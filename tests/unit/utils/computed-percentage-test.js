import computedPercentage from 'new-tmist/utils/computed-percentage';
import { module, test } from 'qunit';

module('Unit | Utility | computed percentage');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = computedPercentage();
  assert.ok(result);
});
